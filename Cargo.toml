[workspace]
members = ["crates/alsie", "crates/engine", "crates/server", "crates/renderer"]
resolver = "3"

[profile.release]
lto = true
panic = "abort"

[workspace.dependencies]
csscolorparser = "0.7"
serde = { version = "1", features = ["derive", "rc"] }
serde_json = "1"
tracing = "0.1"

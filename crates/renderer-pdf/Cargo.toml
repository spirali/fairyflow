[package]
name = "renderer-pdf"
version = "0.1.0"
edition = "2024"

[dependencies]
renderer-core = { path = "../renderer-core" }
krilla = "0.8"
krilla-svg = "0.8"
# usvg 0.47 is required by krilla-svg 0.8; re-parse raw SVG bytes here
usvg = "0.47"
anyhow = { workspace = true }
tracing = { workspace = true }
